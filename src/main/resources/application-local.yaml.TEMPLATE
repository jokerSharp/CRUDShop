spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:online_shop;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: {username}
    password: {password}
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect